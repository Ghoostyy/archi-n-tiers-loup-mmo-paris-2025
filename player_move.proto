syntax = "proto3";

service PlayerMoveService {
  rpc Move (PlayerAction) returns (MoveResponse);
}

message PlayerAction {
  string player_id = 1;
  int32 dx = 2;
  int32 dy = 3;
}

message MoveResponse {
  bool success = 1;
  string message = 2;
  int32 new_x = 3;
  int32 new_y = 4;
}